#!/bin/sh

set -e

_attach_to_session() {
  if [ -z "$TMUX" ]; then
    (
      set -x
      tmux attach -t "$1"
    )
  else
    (
      set -x
      tmux switch-client -t "$1"
    )
  fi
}

_attach_to_session "$@"
